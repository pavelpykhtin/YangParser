namespace YangParser.GrammarModel;

/// <summary>Ordering policy.</summary>
public enum OrderedBy
{
    /// <summary>Entries are ordered by user.</summary>
    User,

    /// <summary>Entries are ordered by system.</summary>
    System
}